{
  "name": "notion-sync",
  "version": "1.0.0",
  "description": "Sync a specific Notion page tree to markdown files in a GitHub repo",
  "type": "module",
  "scripts": {
    "sync": "node sync.js"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "node-fetch": "^3.3.2"
  }
}
