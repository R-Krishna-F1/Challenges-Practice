Also we can use reFlutter to decompile which gives actual code of flutter using the commands 
`reflutter truyencuoiremind_wwf.apk`
In case if you dont have reflutter use the commands `pipx install reflutter`
<empty-block/>
firmware updates are usually stored in res/rax or assets